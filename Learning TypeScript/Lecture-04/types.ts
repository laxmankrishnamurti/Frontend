let greetings: string = "Hello TypeScript!";
console.log(greetings);

// greetings = false; //Type boolean is not assign to type string

console.log(greetings.toUpperCase());

export {}; //Remove Temporary swiggly line issue
